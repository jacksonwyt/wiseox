# Environment variables required for the WiseOx Media website
# Copy this file to .env for local development (if needed) and set the actual values.
# In production (Vercel), set these variables in the project settings.

# Node Environment (Typically set by the platform, e.g., 'production' or 'development')
# NODE_ENV=development

# --- Email Configuration (using Nodemailer) ---

# Example for Google Workspace / Gmail:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465 # Use 465 (SSL) or 587 (TLS)
EMAIL_USER=info@wiseoxmedia.com
EMAIL_PASS=your_google_app_password # Use an App Password if 2FA is enabled
EMAIL_FROM="Wise Ox Contact" <info@wiseoxmedia.com> # Must be an authorized sender

# Example for other SMTP providers:
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@example.com
# EMAIL_PASS=your_email_password
# EMAIL_FROM="Website Contact" <noreply@example.com> 